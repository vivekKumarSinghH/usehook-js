# usehook-js requires no environment variables at the root, in the hooks
# package, or in the CLI — there is no backend, no secrets, no API keys.
# apps/docs adds its own .env.example for a small number of build-time
# public vars once the docs app exists (Story 1.3).
